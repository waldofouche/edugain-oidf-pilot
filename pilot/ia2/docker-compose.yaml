services:
  goia2:
    image: oidfed/lighthouse:0.8.3
    restart: unless-stopped
    volumes:
      - ./lighthouse/config.yaml:/config.yaml:ro
      - ./lighthouse/data:/data
    networks:
      - caddy
      
networks:
  caddy:
    external: true
    name: caddy-network
    
