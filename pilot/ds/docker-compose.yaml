services:
  ds:
    image: edugain-pilot/ds:latest
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - caddy

networks:
  caddy:
    external: true
    name: caddy-network

